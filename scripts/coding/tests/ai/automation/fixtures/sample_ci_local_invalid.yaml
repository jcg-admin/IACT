---
version: "1.0"
metadata:
  project: "test-project"

pipeline:
  name: "Test Pipeline"
  fail_fast: "invalid_boolean"
  timeout: "not_a_number"

stages:
  - name: test
    description: "Testing stage"
    depends_on:
      - nonexistent_stage

    jobs:
      - name: test_ui
        command: "npm test"
        timeout: -10
