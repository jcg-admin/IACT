{
  "id": "cpython-prebuilt",
  "version": "1.0.0",
  "name": "CPython Precompiled",
  "description": "Installs precompiled CPython with PGO+LTO optimizations for fast Dev Container builds",
  "documentationURL": "https://github.com/2-Coatl/IACT---project/blob/main/features/cpython-prebuilt/README.md",
  "licenseURL": "https://docs.python.org/3/license.html",
  "options": {
    "version": {
      "type": "string",
      "default": "3.12.6",
      "description": "CPython version to install (format: X.Y.Z)"
    },
    "artifactUrl": {
      "type": "string",
      "default": "",
      "description": "URL or local path to the precompiled CPython tarball (if empty, uses GitHub Releases)"
    },
    "checksumUrl": {
      "type": "string",
      "default": "",
      "description": "URL or local path to the SHA256 checksum file (if empty, derives from artifactUrl)"
    },
    "installPrefix": {
      "type": "string",
      "default": "/opt/python",
      "description": "Installation prefix for CPython (default: /opt/python)"
    },
    "skipValidation": {
      "type": "boolean",
      "default": false,
      "description": "Skip checksum validation (NOT RECOMMENDED for production)"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}
